.anticon {
  font-size: 28px;
  cursor: pointer;
  line-height: 0;
}

.anticon svg {
  width: 1em;
  height: 1em;
  fill: currentColor;
}

.anticon-spin {
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  0% {
    transform: rotate(0);
  }
  100% {
    transform: rotate(360deg);
  }
}
