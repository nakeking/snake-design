module.exports = {
  // plugins: [  
  //   require('postcss-flexbugs-fixes'),
  //   require("autoprefixer")({
  //     browsers: [
  //       '>1%',
  //       'last 4 versions',
  //       'Firefox ESR',
  //       'not ie < 9', // React doesn't support IE8 anyway
  //     ],
  //     flexbox: 'no-2009',
  //   }),
  //   require("postcss-aspect-ratio-mini"),
  //   require("postcss-write-svg")({ utf8: false }),
  //   require("postcss-cssnext"),
  //   require("postcss-px-to-viewport")({
  //     viewportWidth: 750,     // (Number) 视窗的宽度，对应的是我们设计稿的宽度，一般是750.
  //     viewportHeight: 1334,    // (Number) 视窗的高度，根据750设备的宽度来指定，一般指定1334，也可以不配置.
  //     unitPrecision: 3,       // (Number) 指定 `px` 转换为视图单位值的小数位数(很多情况不能整除).
  //     viewportUnit: 'vw',     // (String) 指定需要转换的视窗单位，建议使用vw.
  //     selectorBlackList: ['.ignore', '.hairlines'],  // (Array) 指定不转换为视窗单位的class,可以自定义，可以无限添加.
  //     minPixelValue: 1,       // (Number) 小于或等于 `1px` 不转换为视窗单位，也可以设置成你想要的值.
  //     mediaQuery: false       // (Boolean) 允许媒体查询中转换 `px`.
  //   }),
  //   require("postcss-viewport-units"),
  //   require("cssnano")({
  //     preset: "advanced",
  //     autoprefixer: false,
  //     "postcss-zindex": false
  //   })
  // ]
}